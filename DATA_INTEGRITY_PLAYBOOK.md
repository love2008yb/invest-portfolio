# 数据质量防错手册 V1.0

> 本文档记录投资决策分析模型在30+轮迭代中犯的所有错误、根本原因分析及防错铁律。
> 每次更新数据前，必须先通读本手册。

---

## 第一部分：错误全景图（共17项，按严重程度排序）

### S级错误（可能导致错误交易决策）

| # | 错误描述 | 实际后果 | 发生次数 |
|---|---------|---------|----------|
| S1 | 华夏半导体成立以来收益写成-5.13%，实际+302.82% | 差点清仓一只优秀基金 | 1 |
| S2 | 港股创新药ETF方向完全相反（+6.94%→-6.46%） | 建仓决策依据完全错误 | 1 |
| S3 | 恒生科技ETF方向完全相反（+3.83%→-4.37%） | 加仓决策依据完全错误 | 1 |
| S4 | 博道沪深300C净值1.8105→实际1.7341 | 盈亏状态由盈变亏未反映 | 1 |

### A级错误（破坏模型可信度）

| # | 错误描述 | 实际后果 | 发生次数 |
|---|---------|---------|----------|
| A1 | 基金代码错误（3只：013566/016299/010481） | 查不到正确基金数据 | 3 |
| A2 | 基金经理信息错误（3人） | 经理评分基于错误对象 | 3 |
| A3 | 调仓建议自相矛盾（推荐+不推荐同一只基金） | 用户无法执行 | 1 |
| A4 | 创新药加仓金额偏差21.5%（30000→24700） | 实际可操作金额不符 | 1 |

### B级错误（影响用户体验）

| # | 错误描述 | 实际后果 | 发生次数 |
|---|---------|---------|----------|
| B1 | 数据源时间错误（混入2025年7月历史数据） | 分析结论完全相反 | 1 |
| B2 | 市值数据被覆盖（份额×新净值覆盖原始市值） | 用户原始数据丢失 | 1 |
| B3 | 融合分析文字改了但positionImpact数据未改 | 分析逻辑与数据脱节 | 1 |
| B4 | A股硬科技S3=红但overallStatus=yellow | 信号系统失效 | 1 |
| B5 | 硬编码fallback数据未同步更新 | 网页显示旧数据 | 3 |
| B6 | 数据来源标注为Wind实际应是天天基金网 | 数据溯源混乱 | 1 |

---

## 第二部分：错误根因分析

### 根因一：数据源管理混乱（导致S1-S4, B1, B6）

**现象**：同一批更新中，部分基金数据正确、部分完全错误。
**分析**：我没有一个固定的数据获取流程。有时从搜索结果中提取、有时从之前的数据中复用、有时标注来源与实际来源不一致。
**根本原因**：缺乏"单一数据源"原则和"逐只核对"的执行纪律。

### 根因二：代码与名称映射未验证（导致A1, A2）

**现象**：3只基金代码错误，3位基金经理信息错误。
**分析**：我直接沿用了用户提供或历史文档中的代码/经理信息，没有独立验证。
**根本原因**：缺乏"代码-名称-经理"三角验证步骤。

### 根因三：修改不同步（导致A3, B3, B5）

**现象**：改了recommendations.ts的文字描述，没改positionImpact数据；改了portfolio.ts的净值，没改组件中的硬编码fallback。
**分析**：一个完整的修改涉及多处文件（数据文件+展示组件+汇总摘要），我只改了其中一处。
**根本原因**：缺乏"修改影响范围检查清单"。

### 根因四：信号规则映射模糊（导致B4）

**现象**：S3=红（恐慌）时overallStatus仍是yellow。
**分析**：信号系统有三层（S1/S2/S3），但overallStatus的判定规则没有严格定义。
**根本原因**：缺乏"信号→状态"的严格映射表。

### 根因五：用户原始数据保护意识薄弱（导致B2）

**现象**：用"份额×新净值"覆盖了用户原始市值。
**分析**：我把市值当成了计算字段，实际上它是用户提供的原始基准数据。
**根本原因**：未区分"用户原始数据（不可更改）"和"派生计算数据（每日更新）"。

---

## 第三部分：防错铁律（共10条，违反任何一条都可能导致错误）

### 铁律1：单一数据源原则

**内容**：所有基金净值必须以天天基金网（fund.eastmoney.com）为唯一数据源。
**执行步骤**：
1. 打开 `https://fund.eastmoney.com/{基金代码}.html`
2. 截图确认"单位净值"右侧日期 = 目标日期
3. 记录单位净值（4位小数）和日增长率（2位小数）
4. 用"当日净值 ÷ 前日净值 - 1"验证涨跌幅
5. 与代码中现有值比对，不一致则更新

**违反后果**：S2, S3, S4（方向相反或数值错误）

### 铁律2：代码-名称-经理三角验证

**内容**：每只基金在录入时必须通过三方交叉验证。
**执行步骤**：
1. 天天基金网页面确认基金全称与代码匹配
2. 页面确认基金经理姓名
3. 与上一次记录比对，如有变更必须搜索确认变更时间和原因
4. 三只数据（代码、名称、经理）必须完全一致才能录入

**违反后果**：A1, A2（代码/经理错误）

### 铁律3：用户原始数据不可更改

**内容**：市值（marketValue）和成本（cost）是用户提供的原始数据，永远不可直接修改。
**正确做法**：
- 更新净值 → 用Python计算新市值 = 份额 × 新净值
- 但只在注释中标注计算过程
- 实际代码中的marketValue字段必须更新为计算值（这是为了显示正确）
- **关键**：成本（cost）字段永远不动

**违反后果**：B2（原始数据丢失）

### 铁律4：修改影响范围检查清单

**内容**：任何数据修改必须检查以下5处是否同步：

```
□ 1. portfolio.ts 中的数据字段
□ 2. recommendations.ts 中的调仓金额/positionImpact
□ 3. 各组件中的硬编码fallback值（ExecutiveSummary/PositionRules/RebalanceTrigger等）
□ 4. 数据来源标注（确保写"天天基金网"而非"Wind"）
□ 5. 信号系统中的联动描述（如跨市场背离的差值）
```

**违反后果**：A3, B3, B5（修改不同步）

### 铁律5：信号→状态严格映射

**内容**：overallStatus不由主观判断，由以下规则表决定：

| S1 | S2 | S3 | overallStatus | 操作 |
|----|----|----|---------------|------|
| 绿 | 绿 | 绿 | green | 加仓/建仓 |
| 绿 | 绿 | 黄 | green | 持有/放缓加仓 |
| 绿 | 绿 | 红 | red | 禁止加仓，持有监控S1 |
| 绿 | 黄 | 任意 | yellow | 不新增暴露 |
| 绿 | 黄 | 红 | red | 减仓无保护暴露 |
| 黄 | 任意 | 红 | red | 减仓/清仓 |
| 红 | 任意 | 任意 | red | 禁止任何操作 |

**铁律**：S3=红（恐慌）时，overallStatus自动=red，没有任何例外。

**违反后果**：B4（信号失效）

### 铁律6：日期时间校核

**内容**：获取任何数据前，必须先校核时间戳。
**执行步骤**：
1. 确认今天的实际日期（2026-07-17）
2. 数据源页面的净值日期必须 = 今天
3. 搜索结果的发布日期必须 = 今天或昨天
4. 任何时间不一致的数据一律丢弃

**违反后果**：B1（历史数据混入）

### 铁律7：金额精确计算

**内容**：任何调仓金额必须用Python精确计算，禁止心算估算。
**执行步骤**：
1. 目标市值 = 总资产 × 目标权重
2. 需加仓金额 = 目标市值 - 当前市值
3. 份额取100的整数倍
4. 实际金额 = 份额 × 净值
5. 操作后的权重用Python验证是否合规

**违反后果**：A4（金额偏差）

### 铁律8：逻辑一致性自检

**内容**：生成任何建议前，必须回答三个问题：
1. 我推荐的基金，我有没有同时说"不推荐"？
2. 我说的加仓金额，计算过程能不能展示？
3. 我说科技合计55%上限，操作后真的不超过55%吗？

**违反后果**：A3（自相矛盾）

### 铁律9：数据来源真实且经过验证

**内容**：
- 数据来源必须标注真实获取的页面/平台
- 当前阶段固定天天基金网（经你验证数据准确）
- 如未来需从Wind/其他平台获取，必须先经过你确认该来源的可靠性
- 禁止：实际从A获取标注为B、不写来源、写模糊来源如"多个数据源综合"

**正确标注**：`// 2026-07-17天天基金网 {净值} {涨跌幅} ✓`

**违反后果**：B6（溯源混乱）

**根本原因说明**：此前标注Wind的数据不准确，不是因为Wind本身不可用，而是我获取的Wind数据有误（可能混入了缓存、错误时间戳或其他偏差）。天天基金网是你验证过数据可靠的来源，所以现阶段固定用它。如需切换，必须经过你确认。

### 铁律10：部署前全量核对

**内容**：构建部署前，必须执行最终核对：
1. 搜索代码中所有硬编码的数字，确认与数据文件一致
2. 用grep搜索旧数值，确认没有残留
3. 浏览器打开部署后的网页，验证关键数字
4. 发现任何不一致 → 停止部署，修正后重新构建

**违反后果**：B5（旧数据残留）

---

## 第四部分：正确路径固化（每次更新必须执行的SOP）

### 每日数据更新SOP（共7步，不可跳过）

```
步骤1: 日期确认
  → 确认今天是2026-XX-XX
  → 搜索"今天是几号"交叉验证

步骤2: 逐只获取净值（按铁律1执行）
  → 9只基金每只单独打开天天基金网页面
  → 每只都要截图+验证日期+验证涨跌幅
  → 记录表格：代码 | 名称 | 净值 | 涨跌 | 来源URL

步骤3: Python全量计算（按铁律7执行）
  → 每只市值 = 份额 × 净值
  → 盈亏 = 市值 - 成本（成本不动）
  → 总资产 = 所有市值之和 + 现金
  → 各资产权重
  → 三层规则检查

步骤4: 三角验证（按铁律2执行）
  → 代码与名称匹配
  → 经理信息确认
  → 与前一日数据比对（涨跌方向是否合理）

步骤5: 全量修改（按铁律4执行）
  → portfolio.ts（净值、市值、盈亏、权重）
  → recommendations.ts（调仓金额、positionImpact）
  → 各组件硬编码fallback
  → 数据来源标注
  → 信号系统描述

步骤6: 逻辑一致性检查（按铁律8执行）
  → 有无自相矛盾
  → 金额计算可展示
  → 操作后权重合规

步骤7: 部署前验证（按铁律10执行）
  → grep搜索旧数值
  → npm run build
  → deploy
  → 浏览器打开验证
```

---

## 第五部分：错误快速自查表

每次用户质疑数据时，按以下顺序排查：

| 排查顺序 | 检查项 | 命令/方法 |
|----------|--------|-----------|
| 1 | 数据来源是否正确？ | 检查是否从天天基金网获取 |
| 2 | 日期是否一致？ | 检查净值日期是否=今天 |
| 3 | 代码-名称是否匹配？ | 天天基金网搜索代码确认全称 |
| 4 | 市值是否被覆盖？ | 检查成本字段是否被动过 |
| 5 | 硬编码是否残留？ | grep搜索旧数值 |
| 6 | 信号映射是否正确？ | 对照铁律5的映射表 |
| 7 | 修改是否同步？ | 逐条核对铁律4检查清单 |

---

## 第六部分：历史错误记录（完整）

| 轮次 | 错误 | 修正时间 | 根因 | 防错铁律 |
|------|------|----------|------|----------|
| 1 | 华夏半导体收益-5.13%→+302.82% | 2026-07-16 | 未交叉验证 | 铁律2 |
| 2 | 基金经理信息3处错误 | 2026-07-16 | 未独立验证 | 铁律2 |
| 3 | 基金代码3只错误 | 2026-07-16 | 未三角验证 | 铁律2 |
| 4 | 调仓建议自相矛盾 | 2026-07-16 | 修改不同步 | 铁律4/8 |
| 5 | 创新药加仓金额偏差21.5% | 2026-07-16 | 未精确计算 | 铁律7 |
| 6 | 科技上限逻辑混乱 | 2026-07-16 | 规则不清晰 | 铁律4 |
| 7 | 数据源时间错误（2025年数据） | 2026-07-17 | 未校核时间戳 | 铁律6 |
| 8 | 市值数据被覆盖 | 2026-07-17 | 未保护原始数据 | 铁律3 |
| 9 | 融合分析文字改数据未改 | 2026-07-17 | 修改范围不清 | 铁律4 |
| 10 | A股硬科技S3=红但status=yellow | 2026-07-17 | 信号映射模糊 | 铁律5 |
| 11 | 港股创新药+6.94%→-6.46% | 2026-07-17 | 数据源非天天基金 | 铁律1 |
| 12 | 恒生科技+3.83%→-4.37% | 2026-07-17 | 数据源非天天基金 | 铁律1 |
| 13 | 博道沪深300 0.00%→-4.22% | 2026-07-17 | 数据源非天天基金 | 铁律1 |
| 14 | 数据来源标注为Wind | 2026-07-17 | 标注不规范 | 铁律9 |
| 15 | 硬编码fallback未同步 | 多次 | 修改范围不清 | 铁律4/10 |
| 16 | 跨市场背离描述错误 | 2026-07-17 | 未用实际数据计算 | 铁律1 |
| 17 | 黄金ETF 8.3541→8.3077 | 2026-07-17 | 数据源偏差 | 铁律1 |

---

*本文档由模型自身根据30+轮迭代中的错误梳理而成，每次数据更新前必须通读。*
*最后更新：2026-07-17*
